# やること

## MVP（今日やる分）

| # | やること | 完了定義 | 状態 |
|---|---|---|---|
| 1 | HTMLファイルを1つ作り、見出しと空の一覧を表示する | ブラウザで開くと「習慣トラッカー」の見出しと、空の一覧の枠が見える | 済 |
| 2 | 入力欄と「追加」ボタンを置き、押したら一覧に行が増えるようにする | 「30分歩く」と入れて追加を押すと、一覧に「30分歩く」の行が増える | 済 |
| 3 | 各行に今日の分のチェックを置き、押すと入る・もう一度押すと外れるようにする | チェックを押すと印が付き、もう一度押すと消える | 済 |
| 4 | 習慣とチェックの状態を配列などのデータとして管理するようにする（画面の見た目は変えず、内部の管理方法だけ変える） | 追加・チェックの操作をしても、これまでと同じように一覧の見た目が変わる（外から見える動きは変化しない） | 済 |
| 5 | そのデータをブラウザに保存し、開き直しても残るようにする | タブを閉じて開き直しても、追加した習慣とチェックの状態がそのまま残っている | 済 |

## 持ち帰る分

| # | やること | 完了定義 | 今日やらない理由 |
|---|---|---|---|
| 6 | 習慣ごとのチェック状態を「今日だけの1つの状態」ではなく「日付ごとの状態」を持つデータ構造に変更する（画面の見た目はまだ変えない） | これまでと同じく今日の列のチェックが追加・削除でき、開き直しても残る（内部のデータ構造だけが変わっている） | 7日表示（#7・#8）のための下ごしらえで、これ単体では画面に変化がなく、今日通す1本の流れには不要なため |
| 7 | 直近7日分の日付を計算し、一覧の見出しに列として並べる | 一覧の見出しに直近7日分の日付が横に並んで表示されている | 週の継続状況を見るための機能で、今日1日分の動作確認には不要なため |
| 8 | 今日の列だけチェックを操作できるようにし、それ以外の日の列はチェック済みなら印を表示するだけの読み取り専用にする | 今日の列は押すとチェックが切り替わる。他の日の列は押しても何も起こらず、過去にチェックした日には印が表示されている | #7が前提の機能で、複数日分のチェック管理は今日のMVPの範囲を超えるため |
| 9 | 各行に「削除」ボタンを置き、押すとその習慣が消えるようにする | 削除を押すと行が消え、開き直しても消えたままになっている | 追加してチェックできれば動くものとして伝わるため、修正・削除の操作は後回しでよいため |
| 10 | 保存が使えない環境のとき、記録が残らないことを画面に出す | プライベートウィンドウで開くと、注意書きが1行表示される | 通常のブラウザ利用では保存が使える前提のため、例外時の対応は後回しでよいため |
| 11 | 見出しは本文より大きく、各行の間に区切り線を入れ、ボタンには枠線か背景色を付けて読みやすくする | 見出しの文字が本文より大きい、一覧の行と行の間に線が見える、ボタンに枠線か背景色が付いている | 機能が一通り動いてから最後に整える方が手戻りが少ないため |

## レビューで出た宿題

| # | 区分 | やること | 重大度 | 今日直さない理由 |
|---|---|---|---|---|
| 12 | セキュリティレビュー | ホスティング先（GitHub Pages / Netlify / Vercelなど）の公開ルート設定が`public/`になっていることを確認する。リポジトリ全体やルート直下が公開対象になっていると、`README.md`・`TASKS.md`・`tetris.html`・`.claude/`まで公開されてしまう | 低 | コード側の修正ではなく実際のデプロイ設定作業のため、講座の進行を優先して今日は着手しない。現状README.md等に秘密情報・個人情報は含まれておらず、誤公開時の実害も小さいと確認済みのため |
| 13 | コードレビュー | habitsへのアクセス方法が、引数渡しとグローバル変数の直接参照で混在している | 低 | 現在のMVP規模では動作上の問題がなく、今日の完成判定にも影響しないため |
| 14 | セキュリティレビュー | `.claude/settings.local.json`にローカルの絶対パスが残っている | 低 | 公開対象は`public/`のみであり、今回の公開には含めないため。リポジトリ整理は後日対応する |

## 今回やらないこと

- 7日より前の履歴を見る — 画面が横に広がるわりに、「続いているか」は7日で十分分かるため
- 連続達成日数（ストリーク）の表示 — 1〜4が動いてからでよい仕上げのため
- 目標回数の設定・リマインド通知 — 通知は「ブラウザだけで動く」範囲を超えるため
- 他の端末との同期・共有 — ログインとサーバーが必要になるため
- 見た目のデザイン（配色・アイコン・アニメーション） — 動くようになってから最後に回すため
